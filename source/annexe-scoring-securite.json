{
  "_": "Security page of the amethyst tool (Scoring). Same shape as the house annexes. Every refs points at a real line of ~/Documents/cascade-scoring at main d3c3d89. The object render does not exist yet (look-dev).",
  "objet": "rendus/etats/objet-scoring-securite.webp",
  "titre": "Where Scoring runs, and what stays on your machine.",
  "lede": "Customer risk scoring reads the most personal table a bank holds. Its public data was written and generated with no real person in it, and no official risk list was copied anywhere. No module is allowed to reach the network.",
  "description": "Customer risk scoring reads a bank's most personal table. Its public data was written and generated with no real person or official list in it.",
  "sections": [
    {
      "h2": "No module may touch the network, and a test checks each source file",
      "html": [
        "<p>Like its sister tools Monitoring and the Dossier, Scoring downloads <b>nothing</b>. The allowlist, the modules allowed to touch the network, is empty. A structural test reads each source file as text and fails the suite if a line touches the network. It first plants a network call and checks that it sees it, so its zero can be trusted.</p>"
      ],
      "verif": {
        "t": "Verify nothing touches the network",
        "p": "Run the suite, which fails if any file gains such a line.",
        "cmd": [
          {
            "c": "npm test",
            "n": "fails if any module touches the network, and the allowlist is empty"
          }
        ],
        "refs": "src/frontiere.test.ts:30 &#183; src/frontiere.test.ts:51"
      },
      "commun": "securite"
    },
    {
      "h2": "The public data holds no real person and no official list",
      "html": [
        "<p>Our public test set is a file we no longer change. Each customer file was <b>written for the typology it illustrates</b>, a named pattern of risk, or generated from those files under a seed. It holds no customer data, anonymised or otherwise.</p>",
        "<p>No official risk list was copied in either. A test requires the suspect files and the retained ones, those not escalated, to <b>share their countries</b>. So the test set cannot bring a list in by its examples. The geography factor, which reads the country, is zero here, and the tool prints that zero as a finding.</p>"
      ],
      "verif": {
        "t": "Check the data's source",
        "p": "The data file states its source, and a test checks that no official list was copied in.",
        "cmd": [
          {
            "c": "python3 -c \"import json; d=json.load(open('src/dossiers-etiquetes.json')); print(d['provenance'], len(d['dossiers']), 'dossiers, each with a written reason')\"",
            "n": "prints: authored 84 dossiers, each with a written reason (source, then customer files)"
          }
        ],
        "refs": "src/dossiers-etiquetes.json:2 &#183; src/dossiers-etiquetes.test.ts:65"
      }
    },
    {
      "h2": "Your two files stay yours, and the outputs carry no value from them",
      "html": [
        "<p>The measurement is a local process at your desk, reading your two CSV files. It loads the reviews your analysts already closed and your customer attributes into memory and writes its outputs <b>next to your files and nowhere else</b>. They are counts, rates, confidence intervals (the range the true rate is likely to sit in) and one answer per review, keyed by review id. Your customer ids, countries and amounts do not leave the machine. Your review ids survive as those keys, so choose opaque ids.</p>",
        "<p>The tool has no callback and no telemetry, so it opens no connection to us and sends no usage data. There is no account to create.</p>"
      ],
      "verif": {
        "t": "Check the test set offline",
        "p": "Cut the network, run everything, then recompute the content hash (the fingerprint of its bytes) of our public test set.",
        "cmd": [
          {
            "c": "CASCADE_OFFLINE=1 npm test",
            "n": "the whole suite with the network cut"
          },
          {
            "c": "npm run sceller -- releve-public.json",
            "n": "prints \"already sealed, and the seal matches\" and the content hash"
          }
        ],
        "refs": "src/sceller.ts:50 &#183; src/dossier.ts:11"
      }
    }
  ]
}
