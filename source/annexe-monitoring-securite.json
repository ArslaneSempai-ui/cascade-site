{
  "_": "Security page of the lapis tool (Monitoring). Same shape as the red annexes. Every refs points at a real line of ~/Documents/cascade-monitoring at main 9f77ea2; the citation guard must know the third repository. The object render does not exist yet (look-dev).",
  "objet": "rendus/etats/objet-monitoring-securite.webp",
  "titre": "Where monitoring runs, and what stays local.",
  "lede": "Transaction monitoring handles the most sensitive table a bank holds. This tool's public data is written and generated, so no module needs the network, and none may reach it. The two files you measure with, your alerts and your transactions, never leave your machine.",
  "description": "Transaction monitoring handles the most sensitive table a bank holds. No module here may reach the network, and your two files stay on your machine.",
  "sections": [
    {
      "h2": "The network allowlist is empty, and a test reads each file",
      "html": [
        "<p>Routing, a sister tool, fetches model weights once and Screening fetches public sanctions lists. Monitoring downloads <b>no file</b>. Its public test set, a frozen file whose content hash (a fingerprint of its exact contents) is checked, is built from written cases and seeded variants, so the allowlist of modules that may touch the network is empty. A source comment beside that list records the same. A structural test reads each source file for network calls (fetch, node:http, WebSockets and the like) and <b>fails the suite if any module gains one</b>. The detector first proves it can see a planted call. So its zero means it looked and found none.</p>"
      ],
      "verif": {
        "t": "Verify the network test",
        "p": "Run the suite. The test fails if any file gains a network call.",
        "cmd": [
          {
            "c": "npm test",
            "n": "fails if any module touches the network, and asserts the allowlist is empty"
          }
        ],
        "refs": "src/frontiere.test.ts:30 &#183; src/frontiere.test.ts:51"
      },
      "commun": "securite"
    },
    {
      "h2": "No real transaction and no real person appear anywhere public",
      "html": [
        "<p>Each account, amount and date in the public test set was <b>written for the typology it illustrates</b> or generated from those cases under a seed. Nothing in it is anonymised customer data, because customer data is absent altogether. The cases file records this in its own header, and each case carries the one-sentence reason it was written.</p>"
      ],
      "verif": {
        "t": "Verify the provenance",
        "p": "The cases file gives its provenance, where each case came from.",
        "cmd": [
          {
            "c": "python3 -c \"import json; d=json.load(open('src/cas-etiquetes.json')); print(d['provenance'], len(d['cas']), 'cases, each with a written reason')\"",
            "n": "prints: authored 84 cases, each with a written reason"
          }
        ],
        "refs": "src/cas-etiquetes.json:2 &#183; src/synthetic.ts:71"
      }
    },
    {
      "h2": "Your two files stay yours, and the outputs carry no value",
      "html": [
        "<p>The measurement runs at your desk. It reads, in memory, your dispositioned alerts (those your analysts already decided) and your transactions, and writes its outputs <b>next to your files and nowhere else</b>. The outputs hold counts, rates, confidence intervals (the range the true rate likely sits in) and verdicts by your alert id. A verdict is the tool's answer on one alert. None of your account ids, amounts, transaction dates or countries leave the machine. Your alert ids do survive as verdict keys, so choose opaque ones.</p>",
        "<p>It makes no callback, sends no telemetry and needs no account. The whole measurement is a local process reading two CSV files (plain text tables) on your machine.</p>"
      ],
      "verif": {
        "t": "Verify on your own decoy",
        "p": "Measure a file holding an invented amount and account id, then search the outputs for them.",
        "cmd": [
          {
            "c": "grep -r \"the amount you invented\" your-alerts-measured.md your-alerts-measured.json",
            "n": "finds no match, because the outputs carry verdicts and none of your values"
          }
        ],
        "refs": "src/your-alerts.ts:9 &#183; src/your-alerts.ts:25"
      }
    },
    {
      "h2": "The test set is hashed, the suite runs offline, and re-measuring needs a visible flag",
      "html": [
        "<p>The public test set is sealed by a content hash, so a figure edited by hand fails loudly. Re-measuring over an existing test set requires an explicit flag on the command line, where an auditor can see it. The whole suite runs with the network cut, and for this tool that is the only way it runs.</p>"
      ],
      "verif": {
        "t": "Verify the content hash, offline",
        "p": "Cut the network, run everything, then recompute the content hash.",
        "cmd": [
          {
            "c": "CASCADE_OFFLINE=1 npm test",
            "n": "the whole suite, network cut"
          },
          {
            "c": "npm run sceller -- releve-public.json",
            "n": "prints \"already sealed, and the seal matches\""
          }
        ],
        "refs": "src/sceller.ts:50 &#183; src/measure.ts:5"
      }
    }
  ]
}
